fastapi>=0.115.0
uvicorn>=0.24.0
pyyaml>=6.0
mcp>=1.0.0
httpx>=0.27.0
cryptography>=42.0.0

# Optional: only required for gold tier (YubiKey mode)
# Not needed for silver tier (phone-only mode)
# fido2>=1.1.0
